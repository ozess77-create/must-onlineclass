rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 로그인한 사용자만 모든 컬렉션에 접근 가능
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
